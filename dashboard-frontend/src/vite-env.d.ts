/// <reference types="vite/client" />

// Standard Vite ambient types. Added by V4 Agent 06 so `import.meta.env.DEV`
// is typed — the Bot Chart logs its payload size in dev builds only, and a
// dev-only measurement must not cost anything in production.
